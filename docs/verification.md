# 第一版验证记录

日期：2026-09-08。环境：Node.js 20.15.1，npm 10.7，桌面 Chromium。

## 工程检查

`npm run check` 通过：ESLint、8 项原生 Node 回归检查、Prettier、Vite 生产构建。锁文件来自公开 npm 源，已实际执行依赖安装。

回归检查覆盖存档损坏及写入失败、高速碰撞与非法物理状态、导航重建、地图出生点、波次与 Boss 轮换、检查点、重开状态、全部敌人类型、重复击杀计分和共享资源释放。没有测试框架依赖。

## 浏览器实际流程

使用生产构建，向测试页面临时注入标准手柄输入，操作真实游戏循环，没有改写玩家生命、敌人或波次状态：

- 开局：生命 120，Rifle 35/175，第 1 波。
- 持续射击：弹匣 35 → 27；装弹后 35/167。
- 切换 Shotgun 并开火：6 → 5 发；Sniper 瞄准出现瞄准镜；切换 Katana 并挥刀。
- 暂停菜单出现，等待后生命和弹药保持不变；点击恢复回到游戏。
- 返回主菜单并重开：恢复 Rifle 35/175、Shotgun 6/36、生命 120 和第 1 波。
- 修复异步开局与动画帧时间戳混用导致的负时间步后，生产版连续 5 次暂停、返回菜单、重开均成功，恢复 Rifle 35 发且没有错误。
- 开发预览中观察到敌人接近、玩家受伤，以及死亡结算菜单。
- 临时抛出异常，页面显示原始错误和“游戏已停止”，没有继续运行或自动重开。

测试页面已重新加载，临时输入和异常注入均未写入源码。

## 验证边界

嵌入式和受自动化控制的浏览器均拒绝了真实 Pointer Lock 请求，页面明确显示浏览器原因。因此没有把虚拟手柄测试视为真实鼠标捕获或实体手柄验收。需在普通浏览器窗口继续人工验证鼠标手感、音效和完整高波次游玩。

首次手柄操作不能替代浏览器音频授权时，菜单明确要求先点击 START / RESUME，不会停在没有反馈的等待状态。

尚未做长时间资源压力测试、所有特殊移动的逐项浏览器验收、完整 Boss 实战或移动设备兼容性测试。

## 联机移植验收

同日新增联机后，`npm run check` 通过，共 17 项回归检查。新增项覆盖协议边界、竞技场出生点、墙体遮挡、房主伤害裁定、重复击杀、复活生命序号、补给争抢、人数上限、胜负条件、格挡与手雷、远端人物资源释放、连接身份和取消握手。依赖审计结果为 0 个已知漏洞。

使用嵌入式浏览器与 Chrome 两个独立客户端完成真实 WebRTC 验证：

- 本地 PeerServer：创建私人房间，访客通过五位房间码加入，双方人数和昵称一致。
- 开始对局并使用临时虚拟手柄进入：双方生命 110，倒计时一致，能够看到另一名玩家的涂鸦模型。
- 为稳定验证命中，将测试玩家临时放在相距 10 米的无遮挡位置，再通过手柄开火。未修改生命或计分：房主真实步枪射击后，访客生命归零，双方计分为房主 1 杀、访客 1 阵亡；随后访客复活，生命恢复 110，生命序号递增。
- 打开对战菜单时对局继续；房主主动离开后，访客收到明确断线提示。
- 创建公开房间 PUB0，先开始对局再让访客加入；访客正确进入“对局已开始”界面。
- 默认 PeerJS 公共信令服务：在 5180 预览创建私人房间，另一个客户端成功入房，双方名单一致。
- 关闭房间后再次加入原房间码，显示连接失败，没有新建房间或切回单机来掩盖失败。

测试房间已退出。所有临时手柄输入、诊断引用和受控站位仅存在于测试页面，已通过重新加载清除，没有写入游戏源码。

尚未验证不同家庭/运营商网络之间的 NAT 连通、实体手柄、10 台设备同时实战、长时间高延迟环境或专用 TURN 中继。上述双客户端测试不等于跨公网多设备验收。

## 手机网页适配验证

日期：2026-09-08。开发预览使用桌面 Chrome 的手机视口与原生多点触摸事件；另一个桌面浏览器作为 PVP 房主。此处不是 iPhone Safari 或安卓真机验收。

工程检查通过：ESLint、24 项 Node 回归检查、Prettier、生产构建。新增 7 项检查覆盖多指独立归属、圆形死区及斜向速度、帧间快速点击、射击拖动、枪械瞄准与持刀格挡、取消手雷蓄力、切换设备、释放捕获及监听器清理、新设置默认值与损坏存档保留。

浏览器验证：

- 原生触摸点击开始进入单人游戏，不申请鼠标捕获。三指同时移动、转向和射击成功，步枪弹匣 35 → 29。
- 瞄准点击后保持开镜；切换武士刀后按住格挡，松手解除；霰弹枪开火 6 → 5。
- 手雷蓄力期间发送触摸取消，蓄力归零、数量保持 4；正常释放后数量 4 → 3。
- 横屏 667×375、844×390、915×412 检查控件边界和截图，按钮无重叠，生命、弹药和对局计时可见。设备像素比设为 2 时，流畅档画布像素尺寸仍等于可见区域，确认上限为 1。
- 竖屏 375×667、390×844、412×915 检查菜单；没有横向溢出，设置与房间操作可滚动访问。旋转至竖屏时暂停并清空输入，竖屏继续会显示提示，转回横屏后仍需点击继续。
- 临时延迟音频启动完成，在等待期间转为竖屏；恢复音频后仍停留菜单并明确提示横屏，没有异步误入战场。
- 使用默认公共信令服务建房；触屏客户端经局域网 IP 地址访问预览并加入电脑房间，双方名单、开局生命和倒计时一致。
- 为稳定验证 PVP 命中，仅将两名测试玩家放在相距 10 米的无遮挡位置；手机模拟端按住射击，电脑端生命归零并复活至 110，双方同步显示 1 次击杀、1 次阵亡。没有直接修改生命或计分。
- 触屏计分板可开关，显示两名玩家。PVP 中转为竖屏显示对战菜单，对局计时继续；双方离房成功。
- 桌面虚拟标准手柄可进入 PVP 和打开菜单；键鼠、手柄、触屏切换的状态清理通过自动化检查。实际鼠标捕获和实体手柄仍受前述验收边界限制。

临时诊断、标准手柄输入及受控站位已通过重新加载测试页面清除，没有写入游戏源码。

### 真机待验收

| 项目           | 当前结果 / 待记录内容                                                                |
| -------------- | ------------------------------------------------------------------------------------ |
| iPhone Safari  | 待提供机型、系统与浏览器版本；单人前 5 波、全部武器和移动技能                        |
| 安卓 Chrome    | 待提供机型、系统与浏览器版本；单人前 5 波、全部武器和移动技能                        |
| 真机与电脑 PVP | 待验证手机建房、加入、击杀、复活、计分板、离房                                       |
| 旋转与后台     | 浏览器旋转模拟通过；真机地址栏、刘海安全区、软键盘、后台挂起和房主停顿超限提示待验证 |
| 性能           | 目标持续 30 FPS；两台真机的实测帧率、卡顿、发热与持续游玩时长均待记录                |
| 桌面实物回归   | 普通浏览器鼠标捕获、键鼠手感和实体手柄待人工验证                                     |

未用桌面模拟帧率或触摸结果代替真机结论。中文操作说明见 [手机网页](mobile.md)。

- [x] 2026-09-08 GitHub Actions 自动部署验证（Deploy workflow e007f3f+）

- [x] 2026-09-08 GitHub Actions 自动部署端到端复验

### 左侧触控遮挡修正

左侧技能、摇杆和疾跑改为同一网格布局，疾跑放在摇杆右侧；操作提示限制在两侧按钮之间，避免压住摇杆。修复原先顶部定位的技能与底部定位的疾跑在可用高度缩小时相撞的问题。

Chrome 布局模拟检查通过：667×375、844×390、915×412、667×320、667×290；另以 844×290 加左右各 44px、底部 21px 的模拟安全区检查。逐对测量按钮、摇杆、生命弹药区及提示文字，均无重叠或越界，并检查截图。临时布局展示样式在测试后随页面重新加载清除；这些结果不替代真机验收。

## 开放世界（PC）验证

日期：2026-09-08。本次以 PC 键鼠为目标，未增加手机适配或开展手机验收。

完整 `npm run check` 通过：ESLint、37 项 Node 检查（原有 24 项，加开放世界 13 项）、Prettier 和生产构建。

新增检查覆盖：

- 不同探索顺序的共享出口、非法地图、道路连通、出生空间、可达性；空白、非法 JSON、截断输出重试，以及鉴权/余额错误不重试。
- 同区生成请求合并、全服务最多两块、保存后重启读取、不重新生成损坏地图。
- 单房主租约、并发进度版本冲突、敌人/奖励进度不能回退、残留临时文件与写入失败保留旧数据。
- 分片重组和大小边界、加载确认、区块卸载后再次按需发送、发送失败可以恢复。
- 房主战斗裁定、重复和旧生命动作忽略、队友手雷免伤、保存响应丢失后的重试不会重复发奖。
- 真实 Player 键盘移动逻辑穿过旧地图 95 米边界、钩索锚点在切换原点后保持世界位置；两个相隔区块的真实敌人组同时模拟并只选择本区玩家。
- 快速移动被未加载边界阻挡，加载后接缝通行；多次远距离往返的几何、碰撞和场景对象保持活动区块规模，轻量探索目录保留。
- 本机 HTTP 处理器拒绝跨站/无会话访问、不同房主争抢写入权，以及中文 JSON 在字节分片处正确解析。

生产构建浏览器联调：

- 创建“验证用城镇”，磁盘营地保存成功；退出、刷新后存档仍出现在继续列表并可以读取。
- 两个独立页面通过真实 PeerJS/WebRTC 建房和加入，同一世界、营地及双方昵称同步，显示 2/10 人。
- 开放世界菜单显示桌面设置、邀请与保存退出，未显示触屏灵敏度。营地与边界雾材质载入后没有新增着色器错误。

验收边界：当前进程没有配置 `DEEPSEEK_API_KEY`，因此没有实际发送付费模型请求；模型检查使用受控返回，不能替代真实生成成功率和耗时验收。内嵌浏览器拒绝 PC 鼠标捕获，未绕过浏览器保护，也未改用手机触控代替；因此真实双客户端的键鼠战斗、相反方向连续探索和中途实战加入仍需在普通桌面浏览器完成。长距离检查为逻辑/资源回归，不代表数小时真实 GPU 内存压力测试；未模拟硬件断电，不将残留临时文件与故障写入检查等同于硬件断电验收。

### DeepSeek 真实生成联调

同日配置服务端密钥后，执行 `node --env-file=.env.local scripts/check-world-live.js`，通过真实 HTTPS 请求和本机 HTTP 接口验证生成链路。

初始两轮共 8 次模型请求未通过地图校验：建筑越过接缝、建筑与道路相交、重试产生非轴对齐道路。失败地图均未落盘或开放。随后明确建筑中心点/尺寸规则，提供由共享边界计算的可行道路参考和可选空地，并把具体错误及上一份输出传回模型纠正；没有放宽校验，也没有用固定地图替代失败结果。

修正后第三轮通过：

| 区块   | 建筑 / 掩体 | 模型耗时 | 结果                                 |
| ------ | ----------- | -------- | ------------------------------------ |
| `-1,0` | 7 / 4       | 2.207 秒 | 首次输出通过并保存                   |
| `1,0`  | 8 / 4       | 3.495 秒 | 首次输出通过并保存；区域名“折页庭院” |
| `1,1`  | 12 / 4      | 2.367 秒 | 首次输出通过并保存                   |

- 同时请求 `1,0` 两次和 `-1,0` 一次，实际生成任务为两个，同区结果完全一致；生成并发峰值为 2。
- 校验三块完整布局、出生点与可达性；验证东西、南北道路出口吻合。实际物理引擎中以 48 米/秒移动，未加载前被边界阻挡，加载真实 AI 地图后可以跨越接缝。
- 关闭服务实例，创建新的服务和存储实例，再通过 HTTP 重新打开全部四块（含营地）：布局和 SHA-256 完全一致，没有调用模型。
- 成功轮共 3,587 tokens；加上前两轮失败尝试，本次真实联调共 11 次模型请求、13,263 tokens。没有读取或输出密钥内容。
- 样本量为 3 块成功地图，不代表长期生成成功率。两块保留了参考区域名“街区名称”，说明命名质量仍可改善；建筑数量、尺寸和高度由模型实际输出。

成功测试报告及原始模型地图输出保留在 `world-data/live-check-U7hupE9o2coo/`。成功世界另外复制到默认存档目录，UUID 为 `00e3c814-394d-4143-8817-39401d4a92f3`，菜单名称为“DeepSeek 实测城镇”。失败测试目录也保留，未覆盖已有世界。

生产构建页面已成功读取默认存档中的“DeepSeek 实测城镇”，显示“已探索 4 个区域”，包含营地与三片 AI 地图；浏览器错误日志为空。验证后保存退出并释放房主会话，服务保留运行供普通桌面浏览器试玩。

真实生成与持久化的密钥阻塞已解除；前述内嵌浏览器鼠标捕获、PC 双人实战及长时间资源压力验收边界仍然适用。

## 小区域与建筑模板 V2 验证（2026-09-08）

- 保留原有 37 项测试，新增 11 项小区域检查，共 48 项通过。覆盖营地补给去重与距离、七栋建筑入口、全部家具类型下的物理通路和出生空间、V2 存档与 V1 兼容、场景确认与取消、重复交互、跨场景攻击、仓库轮次与重置限制、玩家弹药保留、分散敌人模拟、重复加载资源释放、模型输入上限及错误响应。
- 真实 DeepSeek 完整生成：一份主题、四区户外、七栋室内，12 项全部一次通过，峰值并发 2，共 6287 tokens；首轮主题格式验证失败两次共 962 tokens，未发布错误地图。成功报告位于 `world-data/region-live-047xXvMYry18/report.json`，测试世界 `c03b5d75-19e2-4f08-a1e9-69057e0719f0`。
- 通过本机 HTTP 接口验证世界创建、租约、重复区块／室内请求去重；全部布局通过 SHA-256 和实际 NavGrid 路径检查。关闭测试服务后创建新的存储实例，主题、11 个场景及仓库轮次保持一致，没有重新调用模型。
- 普通 Chrome 双客户端实际加入成功，双方显示同一世界、四区已探索、2/10 玩家；访客无需模型密钥。测试结束正常关闭房间并释放租约。
- 实机限制：自动化控制下的普通 Chrome 和之前的内置浏览器都拒绝鼠标锁定，报告 WrongDocumentError。诊断确认请求的画布仍连接当前文档，且与页面画布相同。实际键鼠移动、战斗和跨建筑联调不能据此标为通过；相关核心逻辑使用真实 Player、EnemyManager、碰撞／导航模块自动验证，不能替代这部分实机验收。
