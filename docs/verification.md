# 第一版验证记录

日期：2026-09-08。环境：Node.js 20.15.1，npm 10.7，桌面 Chromium。

## 工程检查

`npm run check` 通过：ESLint、8 项原生 Node 回归检查、Prettier、Vite 生产构建。锁文件来自公开 npm 源，已实际执行依赖安装。

回归检查覆盖存档损坏及写入失败、高速碰撞与非法物理状态、导航重建、地图出生点、波次与 Boss 轮换、检查点、重开状态、全部敌人类型、重复击杀计分和共享资源释放。没有测试框架依赖。

## 浏览器实际流程

使用生产构建，向测试页面临时注入标准手柄输入，操作真实游戏循环，没有改写玩家生命、敌人或波次状态：

- 开局：生命 120，Rifle 35/175，第 1 波。
- 持续射击：弹匣 35 → 27；装弹后 35/167。
- 切换 Shotgun 并开火：6 → 5 发；Sniper 瞄准出现瞄准镜；切换 Katana 并挥刀。
- 暂停菜单出现，等待后生命和弹药保持不变；点击恢复回到游戏。
- 返回主菜单并重开：恢复 Rifle 35/175、Shotgun 6/36、生命 120 和第 1 波。
- 修复异步开局与动画帧时间戳混用导致的负时间步后，生产版连续 5 次暂停、返回菜单、重开均成功，恢复 Rifle 35 发且没有错误。
- 开发预览中观察到敌人接近、玩家受伤，以及死亡结算菜单。
- 临时抛出异常，页面显示原始错误和“游戏已停止”，没有继续运行或自动重开。

测试页面已重新加载，临时输入和异常注入均未写入源码。

## 验证边界

嵌入式和受自动化控制的浏览器均拒绝了真实 Pointer Lock 请求，页面明确显示浏览器原因。因此没有把虚拟手柄测试视为真实鼠标捕获或实体手柄验收。需在普通浏览器窗口继续人工验证鼠标手感、音效和完整高波次游玩。

首次手柄操作不能替代浏览器音频授权时，菜单明确要求先点击 START / RESUME，不会停在没有反馈的等待状态。

尚未做长时间资源压力测试、所有特殊移动的逐项浏览器验收、完整 Boss 实战或移动设备兼容性测试。

## 联机移植验收

同日新增联机后，`npm run check` 通过，共 17 项回归检查。新增项覆盖协议边界、竞技场出生点、墙体遮挡、房主伤害裁定、重复击杀、复活生命序号、补给争抢、人数上限、胜负条件、格挡与手雷、远端人物资源释放、连接身份和取消握手。依赖审计结果为 0 个已知漏洞。

使用嵌入式浏览器与 Chrome 两个独立客户端完成真实 WebRTC 验证：

- 本地 PeerServer：创建私人房间，访客通过五位房间码加入，双方人数和昵称一致。
- 开始对局并使用临时虚拟手柄进入：双方生命 110，倒计时一致，能够看到另一名玩家的涂鸦模型。
- 为稳定验证命中，将测试玩家临时放在相距 10 米的无遮挡位置，再通过手柄开火。未修改生命或计分：房主真实步枪射击后，访客生命归零，双方计分为房主 1 杀、访客 1 阵亡；随后访客复活，生命恢复 110，生命序号递增。
- 打开对战菜单时对局继续；房主主动离开后，访客收到明确断线提示。
- 创建公开房间 PUB0，先开始对局再让访客加入；访客正确进入“对局已开始”界面。
- 默认 PeerJS 公共信令服务：在 5180 预览创建私人房间，另一个客户端成功入房，双方名单一致。
- 关闭房间后再次加入原房间码，显示连接失败，没有新建房间或切回单机来掩盖失败。

测试房间已退出。所有临时手柄输入、诊断引用和受控站位仅存在于测试页面，已通过重新加载清除，没有写入游戏源码。

尚未验证不同家庭/运营商网络之间的 NAT 连通、实体手柄、10 台设备同时实战、长时间高延迟环境或专用 TURN 中继。上述双客户端测试不等于跨公网多设备验收。

## 手机网页适配验证

日期：2026-09-08。开发预览使用桌面 Chrome 的手机视口与原生多点触摸事件；另一个桌面浏览器作为 PVP 房主。此处不是 iPhone Safari 或安卓真机验收。

工程检查通过：ESLint、24 项 Node 回归检查、Prettier、生产构建。新增 7 项检查覆盖多指独立归属、圆形死区及斜向速度、帧间快速点击、射击拖动、枪械瞄准与持刀格挡、取消手雷蓄力、切换设备、释放捕获及监听器清理、新设置默认值与损坏存档保留。

浏览器验证：

- 原生触摸点击开始进入单人游戏，不申请鼠标捕获。三指同时移动、转向和射击成功，步枪弹匣 35 → 29。
- 瞄准点击后保持开镜；切换武士刀后按住格挡，松手解除；霰弹枪开火 6 → 5。
- 手雷蓄力期间发送触摸取消，蓄力归零、数量保持 4；正常释放后数量 4 → 3。
- 横屏 667×375、844×390、915×412 检查控件边界和截图，按钮无重叠，生命、弹药和对局计时可见。设备像素比设为 2 时，流畅档画布像素尺寸仍等于可见区域，确认上限为 1。
- 竖屏 375×667、390×844、412×915 检查菜单；没有横向溢出，设置与房间操作可滚动访问。旋转至竖屏时暂停并清空输入，竖屏继续会显示提示，转回横屏后仍需点击继续。
- 临时延迟音频启动完成，在等待期间转为竖屏；恢复音频后仍停留菜单并明确提示横屏，没有异步误入战场。
- 使用默认公共信令服务建房；触屏客户端经局域网 IP 地址访问预览并加入电脑房间，双方名单、开局生命和倒计时一致。
- 为稳定验证 PVP 命中，仅将两名测试玩家放在相距 10 米的无遮挡位置；手机模拟端按住射击，电脑端生命归零并复活至 110，双方同步显示 1 次击杀、1 次阵亡。没有直接修改生命或计分。
- 触屏计分板可开关，显示两名玩家。PVP 中转为竖屏显示对战菜单，对局计时继续；双方离房成功。
- 桌面虚拟标准手柄可进入 PVP 和打开菜单；键鼠、手柄、触屏切换的状态清理通过自动化检查。实际鼠标捕获和实体手柄仍受前述验收边界限制。

临时诊断、标准手柄输入及受控站位已通过重新加载测试页面清除，没有写入游戏源码。

### 真机待验收

| 项目           | 当前结果 / 待记录内容                                                                |
| -------------- | ------------------------------------------------------------------------------------ |
| iPhone Safari  | 待提供机型、系统与浏览器版本；单人前 5 波、全部武器和移动技能                        |
| 安卓 Chrome    | 待提供机型、系统与浏览器版本；单人前 5 波、全部武器和移动技能                        |
| 真机与电脑 PVP | 待验证手机建房、加入、击杀、复活、计分板、离房                                       |
| 旋转与后台     | 浏览器旋转模拟通过；真机地址栏、刘海安全区、软键盘、后台挂起和房主停顿超限提示待验证 |
| 性能           | 目标持续 30 FPS；两台真机的实测帧率、卡顿、发热与持续游玩时长均待记录                |
| 桌面实物回归   | 普通浏览器鼠标捕获、键鼠手感和实体手柄待人工验证                                     |

未用桌面模拟帧率或触摸结果代替真机结论。中文操作说明见 [手机网页](mobile.md)。

- [x] 2026-09-08 GitHub Actions 自动部署验证（Deploy workflow e007f3f+）
